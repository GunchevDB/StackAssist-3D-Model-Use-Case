// Marquee class to handle scrolling text or elements in our case(Think of parallax)
class Marquee {
  constructor(attributes = {}) {
    this.$element = attributes.element;
  }
}

export default Marquee;
